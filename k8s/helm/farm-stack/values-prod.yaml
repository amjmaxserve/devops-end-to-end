farm-app:

  replicaCount: 5

  hpa:
    minReplicas: 3
    maxReplicas: 5

  image:
    tag: prod

  resources:
    requests:
      cpu: 500m
      memory: 512Mi

    limits:
      cpu: 2
      memory: 2Gi